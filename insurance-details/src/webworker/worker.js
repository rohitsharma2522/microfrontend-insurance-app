// onmessage = function (event) {
//   const { data } = event;
//   // Perform some task
//   const result = data * 2;
//   console.log("result is", data);
//   // Send the result back to the main thread
//   postMessage(result);
// };
